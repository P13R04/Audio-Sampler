{
  "name": "audio-sampler-backend",
  "version": "1.0.0",
  "description": "Backend Node.js/Express pour Audio Sampler - API REST CRUD avec upload de presets",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "dev": "node --watch index.mjs",
    "start": "node index.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs"
  },
  "keywords": [
    "audio",
    "sampler",
    "rest-api",
    "express",
    "upload"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
