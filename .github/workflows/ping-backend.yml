name: Keep Render Backend Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # toutes les 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render backend
        run: |
          curl -s https://audio-sampler-x9kz.onrender.com/api/health
